using System;
using System.Web.Mvc;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using MotorcycleNewb.Controllers;

namespace MotorcycleNewb.Tests.Controllers
{
    [TestClass]
    public class ProfilesControllerTest
    {
        /*
         * ProfilesController controller;

        [TestInitialize]
        public void Initialize()
        {
            controller = new ProfilesController(new MockUnit(new MockDb()));
        }
        [TestCleanup]
        public void Cleanup()
        {
            controller = null;
        }

        [TestMethod()]
        public void EditCheckIfBadRequestReturnTest()
        {

            // Act
            var actual = controller.Edit('s');

            // Assert
            Assert.IsInstanceOfType(actual, typeof(HttpStatusCodeResult));
            var httpResult = actual as HttpStatusCodeResult;
            Assert.AreEqual(400, httpResult.StatusCode);
            Assert.AreEqual("An error occurred whilst processing your request.", httpResult.StatusDescription);
        }
        */
    }
}
